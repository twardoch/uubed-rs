# wheel-matrix

The wheel workflow builds Linux, macOS and Windows artifacts. This consolidation was directly verified on macOS arm64 with CPython 3.12. A successful local build does not verify other platforms.
