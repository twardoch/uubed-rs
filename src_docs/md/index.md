# Uubed native libraries

The Cargo workspace contains uubed-core, uubed-tm and uubed-python.
See the repository README for build instructions and the enclosing workspace
README for book alignment and translation-memory workflows. Generate Rust API
documentation with `cargo doc --no-deps -p uubed-core -p uubed-tm`.
